import {Navbar, Container} from "react-bootstrap";

export default function NavBar() {

    return <div>
        <Navbar>
            <Container>
                <Navbar.Brand>

                </Navbar.Brand>
            </Container>
        </Navbar>
    </div>
}